language: node_js
node_js:
  - "12"
os: linux
dist: bionic

env:
  global:
    - IMAGE_NAME=jsloan117/fluentd

notifications:
  email: false

before_install:
  - npm install semver

jobs:
  include:
    - stage: build docker images
      services: docker
      script: .travis/build-test-push.sh
      cache:
        npm: true
